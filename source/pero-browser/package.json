{
  "name": "pero-browser",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "version": "node scripts/version.js",
    "dev": "npm run version && vite build --watch",
    "build": "npm run version && vite build"
  },
  "dependencies": {
    "@floating-ui/dom": "^1.7.4"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.36",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}